# Business Task: Static site upload flow

Implement static JS/HTML upload flow.

User uploads a marked session archive.
Browser parses it locally.
Browser displays extracted agents, workflows, specs, recommendations.
User can download generated output zip.

Constraints:
- no backend;
- no secrets;
- local-first;
- synthetic fixtures only;
- should work by opening index.html.
