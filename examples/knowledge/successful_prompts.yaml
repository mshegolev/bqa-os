# Synthetic example of the v1 artifact produced by `bqa build` at
# .bqa/knowledge/successful_prompts.yaml
#
# Shape matches the v1 schema in internal/core/knowledge (renderFindings) and
# docs/knowledge-artifacts.md: a common envelope (schema_version / kind /
# generated_by) followed by a `patterns:` list. Each pattern has:
#   id / name / domain / evidence / source / reusable_check / confidence
# For prompts, reusable_check echoes the reusable prompt text itself.
#
# All records below are SYNTHETIC and SAFE — no client data, no secrets.
schema_version: 1
kind: successful_prompts
generated_by: bqa web
patterns:
  - id: "prompts-a1b2c3d4"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: send malformed args to order(id:) and assert a typed GraphQL error instead of a 500; verify the error path and status"
    source: "normalized/graphql/session-01.txt"
    reusable_check: "For each GraphQL mutation/query under [focus], send missing-required-arg, wrong-type, and null-vs-absent variants; assert a typed error with the correct path and no 5xx."
    confidence: high

  - id: "prompts-b2c3d4e5"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: re-run the search API regression suite and verify response schema, status codes, and sort order are unchanged across the release"
    source: "normalized/api/session-02.txt"
    reusable_check: "Given the [endpoint] contract, build a regression check that asserts response shape, status codes, and sort order are unchanged between [baseline] and [candidate] builds, ignoring volatile values."
    confidence: high

  - id: "prompts-c3d4e5f6"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: validate etl reconciliation for the daily partition so destination row count equals source extract count; verify no duplicate business keys"
    source: "normalized/etl/session-03.txt"
    reusable_check: "For pipeline [name], reconcile [src] -> [dst] for partition [date]: assert destination count equals source extract count and no business keys are duplicated."
    confidence: high

  - id: "prompts-d4e5f6a7"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: assert no duplicate business keys in the partition and verify that max(load_ts) is within the freshness SLA; implement the acceptance check"
    source: "normalized/data_quality/session-04.txt"
    reusable_check: "For [dst], emit zero-rows-on-success assertions for: duplicate business keys within [partition], nulls in required columns, and max(load_ts) within [sla] hours."
    confidence: high

  - id: "prompts-e5f6a7b8"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: convert a duplicate-rows bug report into a standardized summary plus a single regression guard that would have caught it; verify it fails pre-fix"
    source: "normalized/bugs/session-05.txt"
    reusable_check: "Turn this bug report into: standardized summary (title/severity/component), minimal repro with [placeholder] inputs, and one regression guard (test or validation rule) that detects the same defect class."
    confidence: high

  - id: "prompts-f6a7b8c9"
    name: "successful_prompt_candidate"
    domain: "prompts"
    evidence: "task: distill a testing session transcript into id/name/domain/evidence/source findings with acceptance context, dropping polite filler"
    source: "normalized/prompts/session-06.txt"
    reusable_check: "Extract reusable findings from [transcript]; for each emit name/domain/evidence/source plus reusable_check; only emit on concrete evidence; drop generic or polite text; replace any sensitive value with [placeholder]."
    confidence: high
